Security note: GET /tool-calls/{id} exposes retained complete arguments and results to every client that can reach the existing Web Access listener. The route inherits current bind/TLS/CORS behavior and adds no authentication. Retention is bounded to 512 KiB per field, 32 MiB per project, and the existing 200-entry history.
